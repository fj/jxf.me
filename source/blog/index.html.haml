---
title: "TODO"
blog: blog
pageable: true
layout: layout_blog
---
-page_articles.sort_by{|a| a.date }.reverse.each_with_index do |article, i|
  %article{class: article.data.link ? "blog link" : "blog"}
    %div{:class => 'grid grid-pad'}
      %div{:class => 'col-3-12'}
        %p
          =dt article.date
          %div{:class => 'tags'}=tag_list article.tags
      %div{:class => 'col-9-12'}
        %h1.post-title
          <i class="fa fa-chain"></i>
          =blog_link article
        =article.summary(250, "...")
  -if paginate
    %span.pagination
      -if prev_page
        %span.prev_page
          =link_to '', prev_page, :id => 'prev_page_link'
      -if next_page
        %span.next_page
          =link_to '', next_page, :id => 'next_page_link'